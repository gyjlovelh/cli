import { Component, OnInit } from "@angular/core";

@Component({
    selector: '{{module.production}}-{{module.name}}',
    templateUrl: './{{module.name}}.component.html',
    styleUrls: ['./{{module.name}}.component.scss']
})
export class {{module.camelName}}Component implements OnInit {

    constructor() {
        // 构造函数初始化内部变量
    }

    ngOnInit() {
        // 初始化
    }

}
